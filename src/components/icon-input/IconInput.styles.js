import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  root: {
    width: '50%',
    height: '10vh',
    marginBottom: '3.5em',
    paddingTop: '0',
  },
}));

export default useStyles;
