import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  root: {
    minWidth: '20rem',
    width: '20rem',
    justifySelf: 'start',
    textAlign: 'right',
    direction: 'rtl',
  },
}));

export default useStyles;
