import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  root: {
  },
  title: {
    fontSize: '1.3em',
    fontWeight: '700',
    textAlign: 'center',
  },
  field: {
    fontSize: '0.9em',
  },
}));

export default useStyles;
