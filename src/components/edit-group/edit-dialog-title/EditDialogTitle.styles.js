import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  root: {},
  title: {
    display: 'flex',
    alignItems: 'center',
    marginTop: '0.2em',
    marginRight: '7rem',
    width: '100%',
  },
}));

export default useStyles;
