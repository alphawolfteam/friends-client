import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  root: {},
  title: {
    direction: 'rtl',
    fontSize: '0.85rem',
  },
}));

export default useStyles;
