import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  root: {
    width: '50%',
    height: '10vh',
    marginBottom: '6rem',
    paddingTop: '0',
  },
}));

export default useStyles;
