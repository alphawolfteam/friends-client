import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  root: {
    width: '50.5%',
    height: '10vh',
    marginBottom: '30%',
    marginTop: '0',
  },
}));

export default useStyles;
