import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  root: {
  },
  title: {
    fontSize: '1.2em',
    fontWeight: '700',
    textAlign: 'center',
  },
  field: {
    fontSize: '1em',
  },
}));

export default useStyles;
