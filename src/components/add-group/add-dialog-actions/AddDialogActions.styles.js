import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  root: {},
  button: {
    fontSize: '1em',
    paddingRight: '1em',
    paddingLeft: '1em',
  },
  icon: {
    marginRight: '0.25em',
    fontSize: '1.5em',
  },
}));

export default useStyles;
