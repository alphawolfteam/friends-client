import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  button: {
    fontSize: '0.9em',
    padding: '0.3em 1em',
  },
  icon: {
    marginRight: '0.25em',
    fontSize: '1.5em',
  },
}));

export default useStyles;
